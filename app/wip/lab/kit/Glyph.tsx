/**
 * The kit graduated: the spine layout won the lab and Glyph now ships with the
 * live course. The lab keeps this path so its ten layouts stay runnable.
 */
export { Glyph, HeadingGlyph, ProviderMark, ProviderStrip } from '@/app/components/course/kit/Glyph';
