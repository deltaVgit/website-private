/**
 * The kit graduated: the spine layout won the lab and these assets now ship
 * with the live course. The lab keeps this path so its ten layouts stay
 * runnable.
 */
export {
  PROVIDERS,
  providerSrc,
  iconSrc,
  providersIn,
  headingIcon,
  type ProviderId,
  type Tone,
} from '@/app/components/course/kit/assets';
